interface:
  display_name: "System Architect"
  short_description: "Route changes into owned modules and protect shared code"
  default_prompt: "Use $system-architect to assign this change to one module, preserve shared surfaces, and define the interfaces needed for integration."
